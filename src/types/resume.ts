// TODO: Define Resume, Experience, Education, Skill, Language, Certification types.
// This is the contract every other branch builds against.
// Filled in chore/data-model branch — do not modify without team review.

export interface Resume {
  id: string;
}
